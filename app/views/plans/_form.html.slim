= simple_form_for plan.object do |f|
  .form-inputs
    = f.input :name
    = f.input :amount
    = f.select :interval, options_for_select([["weekly", "week"], ["monthly", "month"], ["yearly", "year"]])
  .form-actions
    = f.button :submit
