- if policy(comment).edit?
  = link_to t('app.button.edit'), edit_post_comment_path(comment.post, comment), class: 'button success'
- if policy(comment).delete?
  = link_to t('app.button.delete'), post_comment_path(comment.post, comment), class: 'button alert', method: :delete
